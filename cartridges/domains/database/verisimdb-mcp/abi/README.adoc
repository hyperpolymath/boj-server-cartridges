// SPDX-License-Identifier: MPL-2.0
// Copyright (c) 2026 Jonathan D.A. Jewell (hyperpolymath) <j.d.a.jewell@open.ac.uk>
= verisimdb-mcp / abi — Idris2 ABI layer
:orientation: deep

== Purpose

Protocol types for the octadic provenance database. Pins the **"exactly 8
fields"** property of an octad at the type level (`Vect 8 String`), carries
an audit-range ordering proof, and defines the drift-detection vocabulary
(`NoDrift` vs `DriftDetected Nat` — the natural number is the count of
drifted fields).

== Files

[cols="1,4"]
|===
| File | Role

| `VeriSimDB/Protocol.idr`     | Type alias `Octad = Vect 8 String`. Enums `VeriSimDBOp`, `DriftStatus` (`NoDrift`, `DriftDetected Nat`). Records `AuditQuery` (`fromTs`, `toTs`, `prf : LTE fromTs toTs`) and `StoredOctad` (hash, octad). Lemma `octadLength : length o = 8`. Helper `auditRangeValid`.
|===

No `.ipkg`. Consumed directly by wider VeriSim callers.

== Invariants

* **Octad arity** — `Octad = Vect 8 String`. `octadLength : length o = 8`
  (lines 45–46) is the witness; nothing with fewer or more fields can be
  an `Octad`.
* **Audit range ordering** — `AuditQuery.prf : LTE fromTs toTs` (line 32).
  An inverted range cannot be constructed.
* `DriftDetected` carries the count as a `Nat` — drift without a count is
  not representable.

== Test/proof surface

Thin module, no inline tests. Type-checks inside any consuming `.ipkg`.

== Read-first

. Lines 12–42 — data and record definitions (`Octad`, `DriftStatus`,
  `AuditQuery`, `StoredOctad`).
. Lines 45–51 — the two lemmas that travel with this protocol.
